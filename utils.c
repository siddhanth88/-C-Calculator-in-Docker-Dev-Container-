#include <stdio.h>
#include "utils.h"

void greet(const char* name) {
    printf("Welcome, %s!\n", name);
}

int add(int a, int b) {
    return a + b;
}

int subtract(int a, int b) {
    return a - b;
}
